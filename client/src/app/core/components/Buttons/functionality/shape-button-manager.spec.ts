import { ShapeButtonManager } from './shape-button-manager';

describe('ShapeButtonManager', () => {
  it('should create an instance', () => {
    expect(new ShapeButtonManager()).toBeTruthy();
  });
});
